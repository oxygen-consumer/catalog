spring:
  application:
    name: catalog

  datasource:
    # cursed
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver